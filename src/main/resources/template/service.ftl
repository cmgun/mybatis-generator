package ${service.classPackage};

import ${service.basePackage};
import ${entity.classPackage}.${entity.className};

public interface ${service.className} extends ${service.baseName}<${entity.className}> {

}